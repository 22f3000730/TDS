name: Matrix Build with Artifacts - a77a20c

on:
  push:
    branches: [ main ]
  workflow_dispatch: {}

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os: [ubuntu-latest, windows-latest, macos-latest]

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: matrix-a77a20c
        run: |
          echo "Matrix identifier: matrix-a77a20c"
          echo "Building on ${{ matrix.os }} (RUNNER_OS: $RUNNER_OS)"

      - name: Generate build artifact
        run: |
          echo "Build artifact for ${{ matrix.os }} (run $GITHUB_RUN_NUMBER)" > build-a77a20c-${{ matrix.os }}.txt
          echo "GITHUB_RUN_ID=$GITHUB_RUN_ID" >> build-a77a20c-${{ matrix.os }}.txt
          echo "JOB=${{ github.job }}" >> build-a77a20c-${{ matrix.os }}.txt

      - name: Show artifact contents
        run: |
          echo "== Artifact content (build-a77a20c-${{ matrix.os }}.txt) =="
          # cat works on Linux/macOS; on Windows use type if available
          (cat "build-a77a20c-${{ matrix.os }}.txt") 2>/dev/null || (type "build-a77a20c-${{ matrix.os }}.txt")

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: build-a77a20c-${{ matrix.os }}
          path: build-a77a20c-${{ matrix.os }}.txt
